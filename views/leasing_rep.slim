doctype html
html
head
  == render :slim, :'header'
  title Leasing Report
body
  div style="width:80%; min-width:700px; margin:0 auto;"
    h1 Leasing Report

      table.table.table-striped
        thead
          th Month
          th Category
          th No. of Apartments
        tbody
        - for leasingrep in @leasingrep do
          tr
            td.mth = "#{leasingrep["Month"]}"
            td.cat = "#{leasingrep["Pref_apt_category"]}"
            td.num = "#{leasingrep["No of Apartments"]}"
